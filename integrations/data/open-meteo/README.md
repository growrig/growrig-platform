# Open-Meteo

Provides hourly outdoor temperature, relative humidity, and surface pressure
for GrowRig dashboards, weather overlays, and stored weather history.

GrowRig creates and binds this integration automatically because weather is an
existing built-in feature. No credentials are required.
