# Harvest & storage — jars and bags; labels; drying and trimming supplies.
label: Harvest & storage
description: Jars, bags, labels, drying and trimming supplies.
icon: package
order: 5
units: [pcs, packs]
columns:
  - { key: kind, label: Kind, type: enum, options: [jar, bag, label, drying, trimming] }
  - { key: size, label: Size, type: text }
  - { key: material, label: Material, type: text }
